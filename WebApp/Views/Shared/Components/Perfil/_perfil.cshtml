@using Domain
@model Domain.Pessoa
@{
    var foto = string.IsNullOrWhiteSpace(Model.Foto) ? "semfoto.jpg" : @Model.Foto;
    var url = Url.Action("Perfil", "Pessoa");
    //if (!Base.GetDadosUsuario<bool>("IsSuporte"))
    //{
    //    url = Url.Action("Dados", "Pessoa");
    //}
}
<ul class="navbar-nav my-lg-0">
    
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle waves-effect waves-dark" href="" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><img src="/images/pessoa/@foto" alt="user" class="profile-pic"></a>
        <div class="dropdown-menu dropdown-menu-right animated flipInY">
            <ul class="dropdown-user">
                <li>
                    <div class="dw-user-box">
                        <div class="u-img"><a asp-action="AlterarFoto" asp-controller="Pessoa" class="alterar" title="Alterar foto" data-original-title="Alterar foto"><img src="/images/pessoa/@foto" alt="user"></a></div>
                        <div class="u-text">
                            <h4>@Model.Nome</h4>
                            <p class="text-muted">@Model.Email</p>
                        </div>
                    </div>
                </li>
                <li role="separator" class="divider"></li>
                <li><a href="@url" title="Alterar perfil" class="alterar" data-original-title="Alterar perfil"><i class="ti-user"></i> Meu Perfil</a></li>
                @* <li><a href="#"><i class="ti-wallet"></i> My Balance</a></li>
                <li><a href="#"><i class="ti-email"></i> Inbox</a></li>
                <li role="separator" class="divider"></li>
                <li><a href="#"><i class="ti-settings"></i> Account Setting</a></li>*@
                <li role="separator" class="divider"></li> 
                <li><a href="@Url.Action("LogOff", "Pessoa")"><i class="fa fa-power-off"></i> Sair</a></li>
            </ul>
        </div>
    </li>
</ul>